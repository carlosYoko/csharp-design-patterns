@{
    decimal totalLocal = (decimal)ViewBag.totalLocal;
    decimal totalForeign = (decimal)ViewBag.foreignEarn;
}

<div class="row">
    <div class="col-12">
        <label>Total local</label>
        <b>@totalLocal</b> 
        </br>
        <label>Total extranjeros</label>
        <b>@totalForeign</b>
    </div>
</div>